/** Shared Lucide defaults — olive/brown stroke weight matches site chrome. */
export const ICON_STROKE = 1.75;

export const iconProps = (size = 20) => ({
  size,
  strokeWidth: ICON_STROKE,
  "aria-hidden": true,
});
